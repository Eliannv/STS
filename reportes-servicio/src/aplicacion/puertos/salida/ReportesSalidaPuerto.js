export default class ReportesSalidaPuerto {
  leer(servicio, ruta, query, contexto) { throw new Error(`leer ${servicio}/${ruta} no implementado`); }
  listarTodos(servicio, ruta, query, contexto) { throw new Error(`listarTodos ${servicio}/${ruta} no implementado`); }
  salud() { throw new Error('salud no implementado'); }
}
